{% extends 'base.html.twig' %}

{% block title %}Le Quai Antique - Demander un nouveau mot de passe{% endblock %}

{% block body %}
<div class="reset_container">
    <h2 class="reset_title">Demander un nouveau mot de passe</h2>
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    
    {{ form_start(requestForm) }}
        {{ form_row(requestForm.email, {
            label : "Votre e-mail: "
        }) }}
        <div>
            <small>
                Entrez votre adresse e-mail et nous vous enverrons un lien pour créer un nouveau mot de passe
            </small>
        </div>

        <button class="submit_button">Envoyer l'e-mail</button>
    {{ form_end(requestForm) }}
</div>
{% endblock %}
